interface heroProps {
    slideText: String,
    slideImagen: String
}

const Hero = ({ slideText, slideImagen }: heroProps): JSX.Element => {
    return (
        <div className="container">

        </div>
    )
}

export default Hero;